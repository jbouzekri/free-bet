imports:
    - { resource: config.yml }

framework:
    validation:
        cache: apc

doctrine_mongodb:
    document_managers:
        default:
            metadata_cache_driver: apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console
            channels: gamble_process
