{{ bet.event.getName }}<br />
{{ ('gamble_'~betType.getName)|trans({},'gamble') }} {{ betType.getChoiceLabel(bet.event,bet.choice) }}
<form action="{{ path('gamble_cart_remove_bet',{id:bet.event.id}) }}" method="post">
    <input type="hidden" name="bet_type" value="{{ bet.type }}" />
    <input type="submit" value="Delete" />
</form>
<br />